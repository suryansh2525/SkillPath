# Skillpath

This repo is **just a submission**. It is not a runnable app and contains no project setup — only the Framer code components.

These components are meant to be dropped into **Framer**, not used as a standalone Vite/React project:

- `src/Hero.tsx`
- `src/CoursesSection.tsx`
- `src/Footer.tsx`
- `src/App.tsx` (layout reference: Hero → Courses → Footer)
